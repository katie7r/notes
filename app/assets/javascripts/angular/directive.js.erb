(function() {
    'use strict';

    angular
        .module('app')
        .directive('noteForm', noteForm);

        function noteForm() {
          return {
            restrict: 'E',
            scope: {
              note: '='
            },
            templateUrl: "<%= asset_path('pages/_form.html.slim') %>",
            transclude: true
          }
        }

})();
